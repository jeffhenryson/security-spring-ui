/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegenerateBackupCodesRequest } from '../../models/regenerate-backup-codes-request';
import { TotpConfirmResponse } from '../../models/totp-confirm-response';

export interface RegenerateBackupCodes$Params {
      body: RegenerateBackupCodesRequest
}

export function regenerateBackupCodes(http: HttpClient, rootUrl: string, params: RegenerateBackupCodes$Params, context?: HttpContext): Observable<StrictHttpResponse<TotpConfirmResponse>> {
  const rb = new RequestBuilder(rootUrl, regenerateBackupCodes.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TotpConfirmResponse>;
    })
  );
}

regenerateBackupCodes.PATH = '/auth/2fa/backup-codes/regenerate';
